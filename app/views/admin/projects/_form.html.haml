= form.inputs :name => "Project Info" do
  = form.input :name
  = form.input :website
  = form.input :published_at
  
= form.inputs :name => "GitHub Info" do
  = form.input :github_user
  = form.input :github_repository
  
= form.inputs :name => "Description" do 
  ~ form.input :short_description, :input_html => {:rows => 3}
  = form.input :format, :as => :select, :collection => AlmostHappy::Convertor.for_select, :include_blank => false
  ~ form.input :description, :input_html => {:rows => 10}
  