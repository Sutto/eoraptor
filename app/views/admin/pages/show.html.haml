%h2.title== Viewing #{current_resource_name}

= show_for resource do |r|
  
  = r.attribute :id

  = r.attribute :title

  = r.attribute :published_at

  = r.attribute :format
  
  = r.attribute :in_menu
  
  - if resource.in_menu?
    = r.attribute :position

  = r.attribute :content do
    .content-preview= resource.content_as_html